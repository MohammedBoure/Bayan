# Services Directory (`lib/services`)

This directory contains application services handling persistence, progress tracking, and settings.

## Files:
- [progress_service.dart](file:///C:/Users/moham/Desktop/aze2/lib/services/progress_service.dart): Implements `ProgressService` (`ChangeNotifier`) for storing completed lessons, stars earned, evaluation quiz high scores, sound toggles, tashkeel preferences, and reset capabilities using `SharedPreferences`.
